name: workflow-test

on:
  workflow_dispatch:

jobs:
  print-log:
    runs-on: ubuntu-latest

    steps:
      - name: print log
        run: |
          echo "hello world!"
          echo "This is ci-test repository"
